#!/bin/bash

#curl https://observatorium-xyz-observatorium-api-observatorium.apps-crc.testing/\
#api/metrics/v1/api/v1/query/?query=kube_pod_container_status_running --cert ~/certs/client.pem --key ~/certs/client.key -k

